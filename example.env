BOT_TOKEN = bot_token

# Optional: override the API base URL (default: https://go.api.clashk.ing)
# API_URL = http://localhost:8000

# Optional: use a different token for ClashKingAPI auth (defaults to BOT_TOKEN)
# API_TOKEN = your_api_token
